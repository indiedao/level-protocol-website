import FooterUI from '../components/ui/Footer'

const Template = () => <FooterUI />

export const Footer = Template.bind({})
Footer.args = {}

const Story = {
  title: 'Marketing / Footer',
  component: FooterUI,
}

export default Story
